# Drummer to Drummer — Sponsor Brief

A pageless-PDF style website built from the Q1 Sponsor Brief design.

Single-file site: open [`index.html`](index.html). Images live in [`assets/`](assets/).

## Deploy

This repo is set up to deploy to Netlify directly from GitHub — any push to `main` rebuilds the live site.
